# Configurações do banco de dados PostgreSQL
DB_HOST=postgres          # host do banco (Docker)
DB_PORT=5432             # porta padrão
DB_USER=lsj              # usuário
DB_PASSWORD=lsj123       # senha
DB_NAME=lsj              # nome do banco
DB_POOL_SIZE=10          # conexões no pool
DB_ECHO=false            # log SQL (true/false)
DB_SSLMODE=prefer        # modo SSL
DATABASE_URL=postgresql+psycopg://lsj:lsj123@postgres:5432/lsj
